import 'package:flutter/material.dart';

class Six extends StatelessWidget {
  const Six({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Container(height: 250, width: 250, child: Image.asset('assets/bmw.jpg')),);
  }
}